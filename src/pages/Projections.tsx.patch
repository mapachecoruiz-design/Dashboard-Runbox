--- src/pages/Projections.tsx
+++ src/pages/Projections.tsx
@@ -82,7 +82,7 @@
 
 export const Projections = () => {
   const [configs, setConfigs] = useState<ClientProjectionConfig[]>(initialProjectionsConfig);
-  const [activeTab, setActiveTab] = useState<'tabla' | 'tarjetas' | 'resumen'>('resumen');
+  const [activeTab, setActiveTab] = useState<'resumen' | 'tabla' | 'tarjetas' | 'comparativa' | 'historico'>('resumen');
   
   const [editingId, setEditingId] = useState<string | null>(null);
   const [editForm, setEditForm] = useState<ClientProjectionConfig | null>(null);
@@ -141,17 +141,31 @@
         <div className="flex items-center gap-2 bg-slate-100 p-1 rounded-lg">
           <button 
             onClick={() => setActiveTab('resumen')}
-            className={cn("px-4 py-2 rounded-md text-xs font-bold transition-colors", activeTab === 'resumen' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
+            className={cn("px-3 py-2 rounded-md text-[11px] font-bold transition-colors whitespace-nowrap", activeTab === 'resumen' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
           >
-            <div className="flex items-center gap-2"><LayoutDashboard className="w-4 h-4" /> Resumen</div>
+            Resumen General
           </button>
           <button 
             onClick={() => setActiveTab('tabla')}
-            className={cn("px-4 py-2 rounded-md text-xs font-bold transition-colors", activeTab === 'tabla' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
+            className={cn("px-3 py-2 rounded-md text-[11px] font-bold transition-colors whitespace-nowrap", activeTab === 'tabla' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
           >
-            <div className="flex items-center gap-2"><List className="w-4 h-4" /> Tabla</div>
+            Proyección Mensual
           </button>
           <button 
             onClick={() => setActiveTab('tarjetas')}
-            className={cn("px-4 py-2 rounded-md text-xs font-bold transition-colors", activeTab === 'tarjetas' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
+            className={cn("px-3 py-2 rounded-md text-[11px] font-bold transition-colors whitespace-nowrap", activeTab === 'tarjetas' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
           >
-            <div className="flex items-center gap-2"><FileSpreadsheet className="w-4 h-4" /> Tarjetas</div>
+            Tarjetas por Cliente
+          </button>
+          <button 
+            onClick={() => setActiveTab('comparativa')}
+            className={cn("px-3 py-2 rounded-md text-[11px] font-bold transition-colors whitespace-nowrap", activeTab === 'comparativa' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
+          >
+            Tabla Comparativa
+          </button>
+          <button 
+            onClick={() => setActiveTab('historico')}
+            className={cn("px-3 py-2 rounded-md text-[11px] font-bold transition-colors whitespace-nowrap", activeTab === 'historico' ? "bg-white text-indigo-700 shadow-sm" : "text-slate-500 hover:text-slate-700")}
+          >
+            Histórico
           </button>
         </div>
       </div>
